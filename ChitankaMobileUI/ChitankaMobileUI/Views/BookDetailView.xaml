<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ChitankaMobileUI.Views.BookDetailView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationPage.HasNavigationBar="False"
    Shell.NavBarIsVisible="False"
    mc:Ignorable="d">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.7*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="0.25*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{OnIdiom auto, Phone=0.9*, Tablet=2*}" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <BoxView
                    Grid.ColumnSpan="2"
                    BackgroundColor="Green"
                    CornerRadius="16" />
                <Image
                    x:Name="BookImage"
                    Grid.Column="0"
                    Margin="8"
                    Aspect="AspectFill" />
                <StackLayout Grid.Column="1">
                    <Label
                        x:Name="BookTitle"
                        Margin="8"
                        FontAttributes="Bold"
                        FontSize="Default"
                        HorizontalTextAlignment="Center"
                        LineBreakMode="TailTruncation"
                        TextColor="White" />
                    <StackLayout>
                        <Label FontSize="Small" Text="Автор:" />
                        <Label x:Name="BookAuthor" TextColor="White" />
                    </StackLayout>
                    <StackLayout>
                        <Label FontSize="Small" Text="Година:" />
                        <Label x:Name="BookYear" TextColor="White" />
                    </StackLayout>
                    <StackLayout>
                        <Label FontSize="Small" Text="Език:" />
                        <Label x:Name="BookLanguage" TextColor="White" />
                    </StackLayout>
                </StackLayout>
            </Grid>
            <ScrollView Grid.Row="1" Visual="Material">
                <StackLayout Margin="8">
                    <Label FontSize="Large" Text="Описание" />
                    <Label
                        x:Name="BookDescription"
                        Margin="16,16"
                        FontSize="Body" />
                </StackLayout>
            </ScrollView>
            <Button
                x:Name="DownloadButton"
                Grid.Row="2"
                Margin="8"
                BackgroundColor="DarkSeaGreen"
                BorderColor="Brown"
                BorderWidth="0.25"
                Clicked="DownloadButton_Clicked"
                CornerRadius="4"
                Opacity="0.85"
                Text="Изтегли"
                TextColor="White"
                Visual="Material" />
        </Grid>
    </ContentPage.Content>
</ContentPage>